language: java
env:
  global:
    - MAVEN_OPTS="-XX:MaxPermSize=512m -Xmx1g -Xms1g"
jdk:
  - openjdk7
cache:
  directories:
    - $HOME/.m2
before_script:
  - ulimit -u 4096
script: mvn clean install -Pqulice --batch-mode --errors --quiet
